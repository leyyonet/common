import {strict as assert} from 'assert';
import {describe} from "node:test";
import {leyyo} from "../src";


describe('a', () => {
    it('should a', () => {
        assert.notEqual(leyyo, null);
    });
});